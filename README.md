# Secure Claims Assistant

This project contains a minimal FastAPI backend and example React components for an AI-driven insurance claims assistant. It demonstrates endpoints for document ingestion, coverage analysis, and other placeholder modules along with a simple PII anonymization pipeline.

## Backend

Run the application with:

```bash
uvicorn app.main:app --reload
```

## Tests

Run unit tests using:

```bash
pytest
```
